import 'package:flutter/material.dart';

class GridViewMenuItem extends StatelessWidget {
  const GridViewMenuItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
